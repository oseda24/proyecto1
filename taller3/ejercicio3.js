// Leer un número entero y mostrar todos los divisores exactos del número comprendidos entre 1 y el número leído.

//Entrada:numero que da el usuario
//Salida:lista de los numeros.

let numero = parseInt(prompt("diga un número "));

for (let i = 1; i = numero; i++) {
    if(i == 0){
        console.log(i);
    }
}