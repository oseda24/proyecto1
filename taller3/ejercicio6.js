// Leer un número entero de tres dígitos y mostrar todos los enteros comprendidos entre 1 y cada uno de los dígitos

let numero = parseInt(prompt("digite un número ")); 
for (let i = 99; i < 1000; i++) {
    if(i%10 == 0); 
    if = numero - (numero/10 * 10);
    if i = 1;
    console.log(i);
    console.log ("los numero digitos son ");
    numero = numero /10;
}